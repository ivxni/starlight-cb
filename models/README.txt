Place your AI model files here.

Supported formats:
- .onnx (ONNX Runtime)
- .enc / .onnx.enc (Encrypted ONNX)
- .engine (TensorRT)

The model should be trained for object detection (YOLOv8 format recommended).
